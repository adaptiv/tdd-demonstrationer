1. Skapa testklass MovieListWhenSavingMovie.

2. Första test: shouldPersistNewMovie.
   Vi vill att movieList ska spara en film direkt den läggs till.

3. Skapa en MovieList.
   Skapa en add-metod i MovieList.
   Skapa klass Movie och instans Alien att lägga till.
   Men hur ska den spara?
   
4. Importera statiskt org.mockito.Mockito.*
   Mocka MovieDao-interface
   Skapa interface MovieDao med metoden save.
   Kör testet (FAIL).

5. Implementera add i MovieList som lägger till filmen i sin lista och sparar via DAO.
   Kör testet (OK).

6. Nytt test: shouldRejectExistingMovie
   Vi vill att MovieList inte ska acceptera en film sm redan finns i registret.

7. Samma setup som tidigare test men lägg till verify(mock).exists(movie).
   Verifiera också att inget save sker med verifyNoMoreInteractions.
   Kör testet (FAIL).

8. Implementera en koll i add i MovieList att den bara läggs till om den inte finns.
   Kör testet (OK).

9. Refaktorisera setup i testklassen.
   Skapandet av Movie, MovieList och mocken kan göras i Before.
   Kör testet (OK).
 